aggregated_model_validation:
  function : validate
  kwargs   :
    batch_size : 32

locally_tuned_model_validation:
  function : validate
  kwargs   :
    batch_size : 32

train:
  function : train
  kwargs   :
    batch_size : 32
    epochs     : 1
    metrics    :
    - loss
